pipeline {
    agent any
    stages {
        stage('Check Push') {
            steps {
                // Use 'bat' instead of 'sh' for Windows
                bat 'echo Push event received!'
            }
        }
    }
}

